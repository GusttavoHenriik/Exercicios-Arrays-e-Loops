// Letra A

const letras = ["A", "a", "B", "C", "L", "z"];

for (let letraE of letras) {
    if (letraE === 'E' && 'e'); {
        console.log('Nenhuma letra "E" ou "e" foi encontrada.')
        break
    }
}

// Letra B

//const letras = ["A", "E", "B", "C", "E", "z"];

//let quantidade = 0;

//for (let letrasE of letras) {
 //   if (letrasE === 'E') {
 //       quantidade++;
  //  }
    
//}

//console.log(`Foram encontradas ${quantidade} letras "E" ou "e".`)